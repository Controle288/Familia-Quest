{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "rewrites": [
    { "source": "/((?!assets/|sw.js|manifest.webmanifest|icon.svg).*)", "destination": "/index.html" }
  ]
}
